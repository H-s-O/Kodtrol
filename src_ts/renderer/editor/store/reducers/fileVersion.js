// import appVersion from '../../lib/appVersion'
// @TODO
const defaultState = '0.2.0';

export default (state = defaultState, { type, payload }) => {
  switch (type) {
    default:
      return state;
      break;
  }
};
