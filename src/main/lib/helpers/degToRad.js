/**
 * @function degToRad
 * @description Converts a value in degrees to a value in radians.
 * @param {Number} degrees The input value in degrees 
 * @returns {Number} The corresponding value in radians
 */
export default function degToRad(degrees) {
  return (degrees * (Math.PI / 180));
};
