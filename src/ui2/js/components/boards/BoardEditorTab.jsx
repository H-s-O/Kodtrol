import React from 'react';

import FullHeightCard from '../ui/FullHeightCard';

export default function BoardEditorTab({ id }) {
  return (
    <FullHeightCard>
      <div>BoardEditorTab</div>
    </FullHeightCard>
  )
}
