import React, { Component } from 'react';

import FullHeightCard from '../ui/FullHeightCard';

export default class TimelineEditor extends Component {
  render = () => {
    return (
      <FullHeightCard>

      </FullHeightCard>
    )
  }
}
